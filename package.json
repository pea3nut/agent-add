{
  "name": "agent-add",
  "version": "0.0.1",
  "description": "Cross-host AI Agent Pack installer — install MCP, Skill, Prompt, Command, Sub-agent in one command",
  "repository": {
    "type": "git",
    "url": "https://github.com/pea3nut/agent-get.git"
  },
  "bugs": {
    "url": "https://github.com/pea3nut/agent-get/issues"
  },
  "homepage": "https://github.com/pea3nut/agent-get",
  "bin": {
    "agent-add": "bin/agent-add.js"
  },
  "files": [
    "bin",
    "docs",
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "prepublishOnly": "npm run build",
    "test": "vitest run tests/unit tests/integration",
    "test:contract": "vitest run tests/contract",
    "test:integration": "vitest run tests/integration",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "ai",
    "agent",
    "mcp",
    "cursor",
    "claude"
  ],
  "license": "MPL-2.0",
  "dependencies": {
    "@inquirer/select": "^5.1.2",
    "commander": "^14.0.3",
    "smol-toml": "^1.6.1",
    "yaml": "^2.8.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
